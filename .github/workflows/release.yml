name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: Release version without the v prefix
        required: true
        default: 0.2.0

permissions:
  contents: write
  id-token: write
  attestations: write
  artifact-metadata: write

jobs:
  package-and-attest:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      VERSION: ${{ inputs.version || github.ref_name }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.88.0
        with:
          components: rustfmt,clippy
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: extensions/sessionweft-vscode/package-lock.json

      - name: Normalize version and release gate
        run: |
          version="${VERSION#v}"
          echo "VERSION=$version" >> "$GITHUB_ENV"
          if [[ "$version" == *-rc.* ]]; then
            echo "RELEASE_POLICY=release/release-policy.json" >> "$GITHUB_ENV"
            echo "RELEASE_EVIDENCE=release/evidence/rc-0.1.0.json" >> "$GITHUB_ENV"
            echo "RELEASE_LEVEL=rc" >> "$GITHUB_ENV"
          elif [[ "$version" == "0.2.0" ]]; then
            echo "RELEASE_POLICY=release/ga-policy-0.2.0.json" >> "$GITHUB_ENV"
            echo "RELEASE_EVIDENCE=release/evidence/ga-0.2.0.json" >> "$GITHUB_ENV"
            echo "RELEASE_LEVEL=ga" >> "$GITHUB_ENV"
          elif [[ "$version" == "0.1.0" ]]; then
            echo "RELEASE_POLICY=release/ga-policy-0.1.0.json" >> "$GITHUB_ENV"
            echo "RELEASE_EVIDENCE=release/evidence/ga-0.1.0.json" >> "$GITHUB_ENV"
            echo "RELEASE_LEVEL=ga" >> "$GITHUB_ENV"
          else
            echo "Unsupported release version: $version" >&2
            exit 1
          fi

      - name: Verify release source
        run: |
          cargo metadata --locked --format-version 1 --no-deps > /dev/null
          cargo fmt --all --check
          cargo clippy --workspace --all-targets --locked -- -D warnings
          cargo test --workspace --locked

      - name: Verify VS Code extension
        working-directory: extensions/sessionweft-vscode
        run: |
          npm ci
          npm run check
          npm run compile

      - name: Materialize evidence for tagged commit
        run: |
          python3 scripts/release/materialize-evidence.py \
            --template "$RELEASE_EVIDENCE" \
            --output release/evidence/verified.json \
            --commit "$GITHUB_SHA"
          cargo run -p sessionweft-release-gate --locked -- \
            --policy "$RELEASE_POLICY" \
            --evidence release/evidence/verified.json \
            --level "$RELEASE_LEVEL"

      - name: Generate CycloneDX SBOM
        run: |
          python3 scripts/release/generate-sbom.py \
            --output dist/sessionweft.cdx.json \
            --version "$VERSION" \
            --commit "$GITHUB_SHA"
          python3 -m json.tool dist/sessionweft.cdx.json >/dev/null

      - name: Build reproducible Runtime package
        env:
          RELEASE_POLICY_PATH: ${{ env.RELEASE_POLICY }}
          RELEASE_EVIDENCE_PATH: release/evidence/verified.json
        run: |
          artifact="$(bash scripts/release/package.sh "$VERSION")"
          echo "runtime_artifact=$artifact" >> "$GITHUB_OUTPUT"
        id: package

      - name: Package VS Code client
        run: |
          package="dist/sessionweft-vscode-${VERSION}.tar.gz"
          tar --sort=name --mtime='UTC 2026-01-01' --owner=0 --group=0 --numeric-owner \
            --exclude=node_modules \
            -czf "$package" -C extensions/sessionweft-vscode \
            package.json package-lock.json out media
          sha256sum "$package" > "${package}.sha256"

      - name: Generate provenance and SBOM attestation
        uses: actions/attest@v4
        with:
          subject-path: dist/*.tar.gz
          sbom-path: dist/sessionweft.cdx.json

      - name: Upload release bundle
        uses: actions/upload-artifact@v4
        with:
          name: sessionweft-${{ env.VERSION }}
          path: |
            dist/*.tar.gz
            dist/*.sha256
            dist/sessionweft.cdx.json
            release/evidence/verified.json
            release/adapters/activation.json
            release/adapters/verified/*.json
          if-no-files-found: error
          retention-days: 90

      - name: Publish immutable GitHub release assets
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            dist/*.tar.gz \
            dist/*.sha256 \
            dist/sessionweft.cdx.json \
            release/evidence/verified.json \
            release/adapters/activation.json \
            release/adapters/verified/*.json \
            --verify-tag \
            --generate-notes \
            --title "SessionWeft $VERSION"
